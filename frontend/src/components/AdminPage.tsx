
// import config from '../config'
import _kc from '@/keycloak'

export default function AdminPage() {
  return (
    <div
      style={{
        minHeight: '45em',
        maxHeight: '45em',
        width: '100%',
        marginLeft: '4em',
      }}
    >
      Hello Admin
    </div>
  )
}
